---
version: 3
projects:
  - name: default
    dir: terraform
    workflow: myworkflow

workflows:
  myworkflow:
    plan:
      steps:
        - env:
            name: GOOGLE_APPLICATION_CREDENTIALS
            command: echo /home/atlantis/.gcp/gce-account.json
        - init
        - plan
        - apply
